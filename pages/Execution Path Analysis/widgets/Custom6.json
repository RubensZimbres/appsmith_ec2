{
  "backgroundColor": "#FFFFFF",
  "borderColor": "#E0DEDE",
  "borderRadius": "{{appsmith.theme.borderRadius.appBorderRadius}}",
  "borderWidth": "1",
  "bottomRow": 47,
  "boxShadow": "{{appsmith.theme.boxShadow.appBoxShadow}}",
  "defaultModel": "{{\n  {\n    \"data_scan_results\": QueryCodeScanner.data?.fileData ? JSON.parse(QueryCodeScanner.data.fileData) : {},\n    \"data\": Query1.data?.fileData ? JSON.parse(Query1.data.fileData) : {}\n  }\n}}",
  "dynamicBindingPathList": [
    {
      "key": "theme"
    },
    {
      "key": "borderRadius"
    },
    {
      "key": "boxShadow"
    },
    {
      "key": "defaultModel"
    }
  ],
  "dynamicHeight": "FIXED",
  "dynamicTriggerPathList": [
    {
      "key": "onResetClick"
    }
  ],
  "events": [
    "onResetClick"
  ],
  "isCanvas": false,
  "isLoading": false,
  "isSearchWildcard": true,
  "isVisible": true,
  "key": "pa5izwbplx",
  "leftColumn": 3,
  "maxDynamicHeight": 9000,
  "minDynamicHeight": 4,
  "mobileBottomRow": 31,
  "mobileLeftColumn": 3,
  "mobileRightColumn": 26,
  "mobileTopRow": 1,
  "needsErrorInfo": false,
  "onResetClick": "{{showAlert('Successfully reset!!', '');}}",
  "parentColumnSpace": 19.59375,
  "parentId": "0",
  "parentRowSpace": 10,
  "renderMode": "CANVAS",
  "rightColumn": 61,
  "srcDoc": {
    "css": "@charset \"UTF-8\";\n:root {\n  --bg-color: #f4f7f6;\n  --card-bg: #ffffff;\n  --primary: #2c3e50;\n  --accent: #3498db;\n  --text: #333;\n  --border: #ddd;\n  --critical: #c0392b;\n  --warning: #f39c12;\n  --safe: #27ae60;\n}\n\nbody {\n  font-family: \"Segoe UI\", Tahoma, Geneva, Verdana, sans-serif;\n  background-color: var(--bg-color);\n  color: var(--text);\n  margin: 0;\n  padding: 20px;\n}\n\nheader {\n  text-align: center;\n  margin-bottom: 30px;\n  color: var(--primary);\n  font-size: 1.3rem;\n}\n\nheader p {\n  margin-top: 0;\n  color: #7f8c8d;\n  font-size: 1.1rem;\n}\n\n.stats-grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));\n  gap: 20px;\n  margin-bottom: 30px;\n}\n\n.card {\n  background: var(--card-bg);\n  padding: 20px;\n  border-radius: 8px;\n  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);\n  text-align: center;\n  border-top: 4px solid var(--accent);\n}\n\n.card h3 {\n  margin: 0;\n  font-size: 1.1rem;\n  color: #7f8c8d;\n  text-transform: uppercase;\n}\n\n.card p {\n  font-size: 2.2rem;\n  font-weight: bold;\n  margin: 10px 0 0;\n  color: var(--primary);\n}\n\n.findings-container {\n  background: var(--card-bg);\n  padding: 20px;\n  border-radius: 8px;\n  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);\n  margin-bottom: 30px;\n  border-left: 5px solid var(--critical);\n}\n\n.findings-container h3 {\n  margin-top: 0;\n  color: var(--primary);\n}\n\n.findings-list {\n  list-style-type: none;\n  padding-left: 0;\n  margin: 0;\n}\n\n.findings-list li {\n  padding: 10px 0;\n  border-bottom: 1px solid var(--border);\n  font-size: 1.05rem;\n  line-height: 1.5;\n}\n\n.findings-list li:last-child {\n  border-bottom: none;\n}\n\n.findings-list li::before {\n  content: \"⚠️\";\n  margin-right: 10px;\n}\n\n.table-container {\n  background: var(--card-bg);\n  padding: 20px;\n  border-radius: 8px;\n  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);\n  overflow-x: auto;\n}\n\ntable {\n  width: 100%;\n  border-collapse: collapse;\n  margin-top: 10px;\n}\n\nth, td {\n  text-align: left;\n  padding: 12px;\n  border-bottom: 1px solid var(--border);\n}\n\nth {\n  background-color: #f8f9fa;\n  color: var(--primary);\n}\n\n.badge {\n  padding: 4px 8px;\n  border-radius: 4px;\n  font-size: 0.95rem;\n  font-weight: bold;\n  display: inline-block;\n}\n\n.badge-critical {\n  background: #f8d7da;\n  color: #721c24;\n  border: 1px solid #f5c6cb;\n}\n\n.badge-warning {\n  background: #fff3cd;\n  color: #856404;\n  border: 1px solid #ffeeba;\n}\n\n.badge-safe {\n  background: #d4edda;\n  color: #155724;\n  border: 1px solid #c3e6cb;\n}\n\ncode {\n  background: #eee;\n  padding: 2px 4px;\n  border-radius: 3px;\n  font-family: monospace;\n  font-size: 0.95rem;\n  color: #d63031;\n}",
    "html": "<div id=\"root\" class=\"aue-wrap\">\n  <div class=\"aue-loading\">Waiting for scan data…</div>\n</div>\n\n<style>\n  :root{\n    --ink:#0f172a; --muted:#64748b; --faint:#94a3b8; --line:#e8edf3;\n    --panel:#ffffff; --soft:#f8fafc;\n    --crit:#dc2626; --high:#ea580c; --med:#d97706; --low:#2563eb;\n    --b-low:#16a34a; --b-mod:#ca8a04; --b-elev:#ea580c; --b-high:#dc2626; --b-crit:#991b1b;\n    --accent:#4f46e5;\n    --mono:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;\n    --sans:-apple-system,BlinkMacSystemFont,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif;\n  }\n  *{box-sizing:border-box}\n  html,body{margin:0;background:transparent;font-family:var(--sans);color:var(--ink)}\n  .aue-loading{padding:28px;color:var(--faint);font-size:13px}\n  .aue-wrap{\n    background:var(--panel);border:1px solid var(--line);border-radius:14px;\n    padding:20px 22px;box-shadow:0 1px 2px rgba(15,23,42,.04);\n  }\n  .aue-top{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap}\n  .aue-title{font-size:20px;font-weight:700;letter-spacing:-.01em}\n  .aue-prov{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}\n  .chip{font-size:11px;color:var(--muted);background:var(--soft);border:1px solid var(--line);\n    border-radius:999px;padding:3px 9px;font-weight:500}\n  .chip b{color:var(--ink);font-weight:600}\n  .phases{display:flex;align-items:center;gap:8px;background:var(--soft);\n    border:1px solid var(--line);border-radius:10px;padding:7px 12px}\n  .ph{display:flex;align-items:center;gap:6px;font-size:12px;font-weight:600;color:var(--ink)}\n  .ph .dot{width:7px;height:7px;border-radius:50%;background:#16a34a}\n  .ph small{color:var(--faint);font-weight:500}\n  .arrow{color:var(--faint);font-size:13px}\n  .aue-sub{margin:14px 0 16px;font-size:13px;color:var(--muted);line-height:1.5;\n    padding-left:11px;border-left:3px solid var(--accent)}\n  .aue-main{display:flex;gap:22px;flex-wrap:wrap}\n  .aue-headline{flex:1 1 360px;font-size:15px;line-height:1.7;color:#334155}\n  .aue-headline .n{font-weight:700;color:var(--ink)}\n  .aue-headline .crit{color:var(--crit);font-weight:700}\n  .aue-headline .warn{color:var(--high);font-weight:700}\n  .scorecard{flex:0 0 280px;border:1px solid var(--line);border-radius:12px;\n    padding:16px 18px;background:linear-gradient(180deg,#fff,var(--soft))}\n  .sc-label{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:var(--faint);font-weight:700}\n  .sc-num{display:flex;align-items:baseline;gap:4px;margin:2px 0 4px}\n  .sc-num span{font-size:46px;font-weight:800;line-height:1;letter-spacing:-.03em}\n  .sc-num small{font-size:16px;color:var(--faint);font-weight:600}\n  .sc-band{display:inline-block;font-size:12px;font-weight:700;color:#fff;\n    border-radius:6px;padding:3px 10px;letter-spacing:.02em}\n  .bandbar{position:relative;height:8px;border-radius:5px;overflow:hidden;display:flex;margin:14px 0 6px}\n  .bandbar .seg{height:100%}\n  .marker{position:absolute;top:-4px;width:3px;height:16px;background:var(--ink);\n    border-radius:2px;transform:translateX(-50%);box-shadow:0 0 0 2px #fff}\n  .bandscale{display:flex;justify-content:space-between;font-size:9px;color:var(--faint);margin-bottom:10px}\n  .sc-sub{font-size:11.5px;color:var(--muted);line-height:1.45}\n  .sc-meth{font-size:10.5px;color:var(--faint);margin-top:7px}\n</style>\n\n<script>\n(function(){\n  const pts = {critical:12, high:5, medium:2, low:0.25};\n  const sevRank = {critical:4, high:3, medium:2, low:1};\n  const anchorMap = {4:75, 3:50, 2:28, 1:12, 0:0};\n\n  function compute(model){\n    const scan = (model && model.data_scan_results) || {};\n    const report = (model && model.data) || {};\n    const cr = scan.crossings || {};\n    const byProv = (scan.components && scan.components.summary && scan.components.summary.by_provider) || {};\n    const byType = (scan.components && scan.components.summary && scan.components.summary.by_type) || {};\n    const findings = report.findings || [];\n\n    const crit = cr.critical_count || 0;\n    const high = cr.high_count || 0;\n    const med  = cr.medium_count || 0;\n    const pickle = byProv.Pickle || 0;\n    const pulls = findings.filter(f => f.category === 'hf_import').length || 0;\n\n    const buckets = [\n      {sev:'critical', n:crit},\n      {sev:'high',     n:high},\n      {sev:'high',     n:pickle},\n      {sev:'medium',   n:med},\n      {sev:'low',      n:pulls}\n    ].filter(b => b.n > 0);\n\n    let raw = 0, worst = 0;\n    buckets.forEach(b=>{\n      raw += pts[b.sev] * Math.min(1 + Math.log(b.n), 5);\n      worst = Math.max(worst, sevRank[b.sev]);\n    });\n    const volume = 25 * (1 - Math.exp(-raw/30));\n    const anchor = anchorMap[worst] || 0;\n    const score = Math.round(Math.min(100, anchor + volume));\n\n    // context for headline\n    const reposScanned = report.repos_scanned || (Array.isArray(report.repo) ? report.repo.length : 0);\n    const reposAI = (report.inventory && report.inventory.repos_using_ai || []).length;\n    const crossings = cr.total_crossings || 0;\n    const artifacts = (byType.model_artifact && byType.model_artifact.count) || 0;\n    const scanId = (report.scan_name || '').slice(0,8);\n    const surfaces = (report.surfaces_run || []).map(s=> s==='cicd'?'CI/CD':s).join(' + ');\n\n    // describe the critical crossing\n    let critPhrase = 'a sensitive value reaching an AI boundary';\n    (scan.crossings && scan.crossings.crossings || []).some(c=>{\n      if(c.severity==='critical'){\n        if(c.data_category==='credentials') critPhrase='a database credential reaching an LLM prompt';\n        return true;\n      }\n      return false;\n    });\n\n    return {score, band:band(score), bandColor:bandColor(score),\n      reposScanned, reposAI, pulls, crossings, crit, artifacts, pickle,\n      scanId, surfaces, critPhrase};\n  }\n\n  function band(s){ return s<=20?'Low':s<=45?'Moderate':s<=70?'Elevated':s<=90?'High':'Critical'; }\n  function bandColor(s){ return s<=20?'var(--b-low)':s<=45?'var(--b-mod)':s<=70?'var(--b-elev)':s<=90?'var(--b-high)':'var(--b-crit)'; }\n\n  function render(model){\n    const d = compute(model);\n    const hasData = d.reposScanned || d.crossings || d.pulls;\n    if(!hasData){\n      document.getElementById('root').innerHTML = '<div class=\"aue-loading\">Waiting for scan data…</div>';\n      return;\n    }\n    const segs = [\n      {w:20,c:'var(--b-low)'},{w:25,c:'var(--b-mod)'},{w:25,c:'var(--b-elev)'},\n      {w:20,c:'var(--b-high)'},{w:10,c:'var(--b-crit)'}\n    ].map(s=>`<div class=\"seg\" style=\"width:${s.w}%;background:${s.c}\"></div>`).join('');\n\n    document.getElementById('root').innerHTML = `\n      <div class=\"aue-wrap\">\n        <div class=\"aue-top\">\n          <div>\n            <div class=\"aue-title\">AI Usage &amp; Exposure</div>\n            <div class=\"aue-prov\">\n              <span class=\"chip\">scan <b>${d.scanId||'—'}</b></span>\n              <span class=\"chip\">source <b>github</b></span>\n              <span class=\"chip\"><b>${d.reposScanned}</b> repos</span>\n              <span class=\"chip\">surfaces <b>${d.surfaces||'—'}</b></span>\n            </div>\n          </div>\n          <div class=\"phases\">\n            <span class=\"ph\"><span class=\"dot\"></span>Discovery <small>done</small></span>\n            <span class=\"arrow\">→</span>\n            <span class=\"ph\"><span class=\"dot\"></span>Posture <small>done</small></span>\n          </div>\n        </div>\n\n        <div class=\"aue-sub\">One scan, two phases — <b>Discovery</b> finds where AI is used; <b>Posture</b> scores how risky that use is. Discovery never emits a score; Posture does.</div>\n\n        <div class=\"aue-main\">\n          <div class=\"aue-headline\">\n            AI found in <span class=\"n\">${d.reposAI} of ${d.reposScanned}</span> repos\n            (<span class=\"n\">${d.pulls}</span> model pulls).\n            <span class=\"n\">${d.crossings}</span> flows carry sensitive data into AI boundaries —\n            <span class=\"crit\">${d.crit} critical</span>: ${d.critPhrase}.\n            <span class=\"n\">${d.artifacts}</span> model artifacts ship in code-executing formats\n            (<span class=\"warn\">${d.pickle} unsigned</span>); <span class=\"n\">${d.pulls}</span> pulls are unpinned.\n          </div>\n\n          <div class=\"scorecard\">\n            <div class=\"sc-label\">Posture Score</div>\n            <div class=\"sc-num\"><span style=\"color:${d.bandColor}\">${d.score}</span><small>/ 100</small></div>\n            <div class=\"sc-band\" style=\"background:${d.bandColor}\">${d.band}</div>\n            <div class=\"bandbar\">\n              ${segs}\n              <div class=\"marker\" style=\"left:${d.score}%\"></div>\n            </div>\n            <div class=\"bandscale\"><span>0</span><span>20</span><span>45</span><span>70</span><span>90</span><span>100</span></div>\n            <div class=\"sc-sub\">Severity-weighted · anchored by the single worst finding (1 critical credential → LLM leak).</div>\n            <div class=\"sc-meth\">Full formula in the methodology card at the bottom of the dashboard.</div>\n          </div>\n        </div>\n      </div>`;\n  }\n\nfunction boot(){\n    if (typeof appsmith === 'undefined'){ setTimeout(boot, 50); return; }\n    appsmith.onReady(function(){ render(appsmith.model); });\n    if (appsmith.onModelChange) appsmith.onModelChange(function(m){ render(m); });\n  }\n  boot();\n})();\n</script>",
    "js": "appsmith.onReady(() => {\n  appsmith.onModelChange(model => {\n    const rawData = model.cookies || {};\n    const components = rawData.components || {};\n    const summary = components.summary || {};\n    const byType = summary.by_type || {};\n    const byProvider = summary.by_provider || {};\n    const crossFileData = rawData.cross_file || {};\n    const crossings = rawData.crossings || {};\n    const crossingList = crossings.crossings || [];\n    // =====================================================================\n    // CONFIG — the ONLY values not present in the scan data (presentation /\n    // display policy). The exposure index itself uses NO weighting coefficients.\n    // =====================================================================\n    const CONFIG = {\n      indexCap: 100,\n      indexThresholds: {\n        high: 75,\n        medium: 40\n      },\n      indexColors: {\n        high: '#c0392b',\n        medium: '#f39c12',\n        low: '#27ae60'\n      },\n      badgeBySeverity: {\n        critical: 'badge-critical',\n        high: 'badge-warning',\n        medium: 'badge-warning',\n        low: 'badge-warning'\n      },\n      badgeDefault: 'badge-warning',\n      severityOrder: {\n        critical: 0,\n        high: 1,\n        medium: 2,\n        low: 3\n      },\n      maxActionRows: 5,\n      okColor: '#27ae60',\n      emptyFindings: 'No critical execution dependencies or boundary violations detected.',\n      emptyActions: 'No immediate actions required.',\n      // Serialization provider names — these carry \"High Exposure\" weight.\n      serializationTypes: ['Pickle', 'Joblib', 'H5', 'PT', 'Safetensors']\n    };\n    // --- Helpers ---------------------------------------------------------\n    const fmtCat = s => String(s || '').split('_').map(w => w.length <= 3 ? w.toUpperCase() : w.charAt(0).toUpperCase() + w.slice(1)).join(' ');\n    const cap = s => s ? s.charAt(0).toUpperCase() + s.slice(1).toLowerCase() : s;\n    const sevRank = s => CONFIG.severityOrder[String(s || '').toLowerCase()] ?? 99;\n    const fmtProviders = obj => Object.entries(obj || {}).map(([k, v]) => `${k} (${Array.isArray(v) ? v.length : v})`).join(', ');\n    // --- 1. Top-Level Metrics (all wired to the scan) --------------------\n    // AI boundary crossings is now defined as the SUM of the Sensitive Data\n    // Crossings (the by-category breakdown). This value flows into the card\n    // and the \"How this is calculated\" explanation so both stay consistent.\n    const byCategory = crossings.by_category || {};\n    const sensitiveDataCrossingsSum = Object.values(byCategory).reduce((acc, v) => acc + (Number(v) || 0), 0);\n    // If you meant the total instead of the category sum, use:\n    //   const boundaryCrossings = crossings.total_crossings || 0;\n    const boundaryCrossings = sensitiveDataCrossingsSum;\n    const eligibilityViolations = crossings.prohibited_count || 0;\n    const undeclaredComponents = (byType.llm_framework?.count || 0) + (byType.agent?.count || 0);\n    const artifactCount = byType.model_artifact?.count || 0;\n    // Exposed vendors = providers flagged \"High Exposure\" (serialization formats),\n    // consistent with the Vendor Exposure dashboard. A reference to a standard\n    // AI vendor / API is NOT itself an exposure, so those are excluded here.\n    let exposedVendorCount = 0;\n    Object.keys(byProvider).forEach(name => {\n      if (CONFIG.serializationTypes.includes(name)) {\n        exposedVendorCount++;\n      }\n    });\n    // Overall Exposure Index — raw, unweighted sum of the five underlying counts,\n    // capped at 100. No coefficients: each item contributes its own count.\n    //   boundary + exposed vendors + framework + artifact + eligibility\n    const rawScore = boundaryCrossings + exposedVendorCount + undeclaredComponents + artifactCount + eligibilityViolations;\n    const indexScore = Math.min(CONFIG.indexCap, rawScore);\n    // KPI cards\n    document.getElementById('exec-exposure-index').innerText = `${indexScore}/${CONFIG.indexCap}`;\n    document.getElementById('exec-boundary-crossings').innerText = boundaryCrossings;\n    document.getElementById('exec-violations').innerText = eligibilityViolations;\n    document.getElementById('exec-artifacts').innerText = artifactCount;\n    const indexEl = document.getElementById('exec-exposure-index');\n    if (indexScore > CONFIG.indexThresholds.high) indexEl.style.color = CONFIG.indexColors.high;else if (indexScore > CONFIG.indexThresholds.medium) indexEl.style.color = CONFIG.indexColors.medium;else indexEl.style.color = CONFIG.indexColors.low;\n    // Explanation of the Overall Exposure Index (reconciles with the score above)\n    const exIndexFactors = [['AI boundary crossings', boundaryCrossings], ['exposed vendors', exposedVendorCount], ['undeclared frameworks/agents', undeclaredComponents], ['model artifacts', artifactCount], ['eligibility violations', eligibilityViolations]];\n    const exIndexBreakdown = exIndexFactors.map(([k, v]) => `${v} ${k}`).join(' + ');\n    const exCapNote = rawScore > CONFIG.indexCap ? ` (raw total ${rawScore}, capped at ${CONFIG.indexCap})` : '';\n    document.getElementById('exec-index-explanation').innerText = `Unweighted sum of five scan counts — ${exIndexBreakdown} = ${rawScore}${exCapNote}.`;\n    // --- 2. Executive Findings (every specific derived from the scan) ----\n    const findingsList = document.getElementById('findings-list');\n    findingsList.innerHTML = '';\n    const findings = [];\n    const prohibited = crossingList.filter(c => c.ai_eligibility === 'prohibited');\n    if ((crossings.total_crossings || 0) > 0 && Object.keys(byCategory).length > 0) {\n      const catList = Object.entries(byCategory).map(([k, v]) => `${fmtCat(k)} (${v})`).join(', ');\n      findings.push(`<strong>Sensitive Data Crossings:</strong> ${crossings.total_crossings} flow(s) carry restricted data into AI boundaries — by category: ${catList}.`);\n    }\n    const byBoundary = crossings.by_boundary || {};\n    if (boundaryCrossings > 0 || Object.keys(byBoundary).length > 0) {\n      const bnames = Object.keys(byBoundary).map(fmtCat).join(', ') || 'n/a';\n      findings.push(`<strong>Boundary Exposure:</strong> ${boundaryCrossings} AI boundary node(s) detected; sensitive data observed at: ${bnames}.`);\n    }\n    if (artifactCount > 0) {\n      const artProv = fmtProviders(byType.model_artifact?.providers);\n      findings.push(`<strong>Supply Chain Risk:</strong> ${artifactCount} model artifact(s) using serialization formats exposed to code execution — ${artProv}.`);\n    }\n    if (findings.length === 0) {\n      findingsList.innerHTML = `<li style=\"color: ${CONFIG.okColor};\">${CONFIG.emptyFindings}</li>`;\n    } else {\n      findings.forEach(f => {\n        const li = document.createElement('li');\n        li.innerHTML = f;\n        findingsList.appendChild(li);\n      });\n    }\n    // --- 3. High Priority Action Items (driven entirely by crossings) ----\n    const tbody = document.getElementById('exec-table-body');\n    tbody.innerHTML = '';\n    const actionItems = crossingList.slice().sort((a, b) => sevRank(a.severity) - sevRank(b.severity)).slice(0, CONFIG.maxActionRows).map(c => ({\n      cat: fmtCat(c.data_category) + (c.ai_eligibility ? ` (${c.ai_eligibility})` : ''),\n      ctx: `${c.file}:${c.line}`,\n      severity: cap(c.severity),\n      badge: CONFIG.badgeBySeverity[String(c.severity || '').toLowerCase()] || CONFIG.badgeDefault,\n      action: c.recommendation || '—'\n    }));\n    if (actionItems.length === 0) {\n      tbody.innerHTML = `<tr><td colspan=\"4\" style=\"text-align: center;\">${CONFIG.emptyActions}</td></tr>`;\n      return;\n    }\n    actionItems.forEach(item => {\n      const tr = document.createElement('tr');\n      const tdCat = document.createElement('td');\n      tdCat.innerHTML = `<strong>${item.cat}</strong>`;\n      const tdCtx = document.createElement('td');\n      const code = document.createElement('code');\n      code.textContent = item.ctx;\n      tdCtx.appendChild(code);\n      const tdSeverity = document.createElement('td');\n      const span = document.createElement('span');\n      span.className = `badge ${item.badge}`;\n      span.textContent = item.severity;\n      tdSeverity.appendChild(span);\n      const tdAction = document.createElement('td');\n      tdAction.textContent = item.action;\n      tr.appendChild(tdCat);\n      tr.appendChild(tdCtx);\n      tr.appendChild(tdSeverity);\n      tr.appendChild(tdAction);\n      tbody.appendChild(tr);\n    });\n  });\n});"
  },
  "theme": "{{appsmith.theme}}",
  "topRow": 1,
  "type": "CUSTOM_WIDGET",
  "uncompiledSrcDoc": {
    "css": ":root {\n    --bg-color: #f4f7f6;\n    --card-bg: #ffffff;\n    --primary: #2c3e50;\n    --accent: #3498db;\n    --text: #333;\n    --border: #ddd;\n    --critical: #c0392b;\n    --warning: #f39c12;\n    --safe: #27ae60;\n}\n\nbody {\n    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;\n    background-color: var(--bg-color);\n    color: var(--text);\n    margin: 0;\n    padding: 20px;\n}\n\nheader {\n    text-align: center;\n    margin-bottom: 30px;\n    color: var(--primary);\n    font-size: 1.3rem;\n}\n\nheader p {\n    margin-top: 0;\n    color: #7f8c8d;\n    font-size: 1.1rem;\n}\n\n.stats-grid {\n    display: grid;\n    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));\n    gap: 20px;\n    margin-bottom: 30px;\n}\n\n.card {\n    background: var(--card-bg);\n    padding: 20px;\n    border-radius: 8px;\n    box-shadow: 0 4px 6px rgba(0,0,0,0.1);\n    text-align: center;\n    border-top: 4px solid var(--accent);\n}\n\n.card h3 {\n    margin: 0;\n    font-size: 1.1rem;\n    color: #7f8c8d;\n    text-transform: uppercase;\n}\n\n.card p {\n    font-size: 2.2rem;\n    font-weight: bold;\n    margin: 10px 0 0;\n    color: var(--primary);\n}\n\n.findings-container {\n    background: var(--card-bg);\n    padding: 20px;\n    border-radius: 8px;\n    box-shadow: 0 4px 6px rgba(0,0,0,0.1);\n    margin-bottom: 30px;\n    border-left: 5px solid var(--critical);\n}\n\n.findings-container h3 {\n    margin-top: 0;\n    color: var(--primary);\n}\n\n.findings-list {\n    list-style-type: none;\n    padding-left: 0;\n    margin: 0;\n}\n\n.findings-list li {\n    padding: 10px 0;\n    border-bottom: 1px solid var(--border);\n    font-size: 1.05rem;\n    line-height: 1.5;\n}\n\n.findings-list li:last-child {\n    border-bottom: none;\n}\n\n.findings-list li::before {\n    content: \"⚠️\";\n    margin-right: 10px;\n}\n\n.table-container {\n    background: var(--card-bg);\n    padding: 20px;\n    border-radius: 8px;\n    box-shadow: 0 4px 6px rgba(0,0,0,0.1);\n    overflow-x: auto;\n}\n\ntable {\n    width: 100%;\n    border-collapse: collapse;\n    margin-top: 10px;\n}\n\nth, td {\n    text-align: left;\n    padding: 12px;\n    border-bottom: 1px solid var(--border);\n}\n\nth {\n    background-color: #f8f9fa;\n    color: var(--primary);\n}\n\n.badge {\n    padding: 4px 8px;\n    border-radius: 4px;\n    font-size: 0.95rem;\n    font-weight: bold;\n    display: inline-block;\n}\n\n.badge-critical { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }\n.badge-warning { background: #fff3cd; color: #856404; border: 1px solid #ffeeba; }\n.badge-safe { background: #d4edda; color: #155724; border: 1px solid #c3e6cb; }\n\ncode { background: #eee; padding: 2px 4px; border-radius: 3px; font-family: monospace; font-size: 0.95rem; color: #d63031; }",
    "html": "<div id=\"root\" class=\"aue-wrap\">\n  <div class=\"aue-loading\">Waiting for scan data…</div>\n</div>\n\n<style>\n  :root{\n    --ink:#0f172a; --muted:#64748b; --faint:#94a3b8; --line:#e8edf3;\n    --panel:#ffffff; --soft:#f8fafc;\n    --crit:#dc2626; --high:#ea580c; --med:#d97706; --low:#2563eb;\n    --b-low:#16a34a; --b-mod:#ca8a04; --b-elev:#ea580c; --b-high:#dc2626; --b-crit:#991b1b;\n    --accent:#4f46e5;\n    --mono:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;\n    --sans:-apple-system,BlinkMacSystemFont,\"Segoe UI\",Roboto,Helvetica,Arial,sans-serif;\n  }\n  *{box-sizing:border-box}\n  html,body{margin:0;background:transparent;font-family:var(--sans);color:var(--ink)}\n  .aue-loading{padding:28px;color:var(--faint);font-size:13px}\n  .aue-wrap{\n    background:var(--panel);border:1px solid var(--line);border-radius:14px;\n    padding:20px 22px;box-shadow:0 1px 2px rgba(15,23,42,.04);\n  }\n  .aue-top{display:flex;justify-content:space-between;align-items:flex-start;gap:16px;flex-wrap:wrap}\n  .aue-title{font-size:20px;font-weight:700;letter-spacing:-.01em}\n  .aue-prov{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}\n  .chip{font-size:11px;color:var(--muted);background:var(--soft);border:1px solid var(--line);\n    border-radius:999px;padding:3px 9px;font-weight:500}\n  .chip b{color:var(--ink);font-weight:600}\n  .phases{display:flex;align-items:center;gap:8px;background:var(--soft);\n    border:1px solid var(--line);border-radius:10px;padding:7px 12px}\n  .ph{display:flex;align-items:center;gap:6px;font-size:12px;font-weight:600;color:var(--ink)}\n  .ph .dot{width:7px;height:7px;border-radius:50%;background:#16a34a}\n  .ph small{color:var(--faint);font-weight:500}\n  .arrow{color:var(--faint);font-size:13px}\n  .aue-sub{margin:14px 0 16px;font-size:13px;color:var(--muted);line-height:1.5;\n    padding-left:11px;border-left:3px solid var(--accent)}\n  .aue-main{display:flex;gap:22px;flex-wrap:wrap}\n  .aue-headline{flex:1 1 360px;font-size:15px;line-height:1.7;color:#334155}\n  .aue-headline .n{font-weight:700;color:var(--ink)}\n  .aue-headline .crit{color:var(--crit);font-weight:700}\n  .aue-headline .warn{color:var(--high);font-weight:700}\n  .scorecard{flex:0 0 280px;border:1px solid var(--line);border-radius:12px;\n    padding:16px 18px;background:linear-gradient(180deg,#fff,var(--soft))}\n  .sc-label{font-size:11px;text-transform:uppercase;letter-spacing:.08em;color:var(--faint);font-weight:700}\n  .sc-num{display:flex;align-items:baseline;gap:4px;margin:2px 0 4px}\n  .sc-num span{font-size:46px;font-weight:800;line-height:1;letter-spacing:-.03em}\n  .sc-num small{font-size:16px;color:var(--faint);font-weight:600}\n  .sc-band{display:inline-block;font-size:12px;font-weight:700;color:#fff;\n    border-radius:6px;padding:3px 10px;letter-spacing:.02em}\n  .bandbar{position:relative;height:8px;border-radius:5px;overflow:hidden;display:flex;margin:14px 0 6px}\n  .bandbar .seg{height:100%}\n  .marker{position:absolute;top:-4px;width:3px;height:16px;background:var(--ink);\n    border-radius:2px;transform:translateX(-50%);box-shadow:0 0 0 2px #fff}\n  .bandscale{display:flex;justify-content:space-between;font-size:9px;color:var(--faint);margin-bottom:10px}\n  .sc-sub{font-size:11.5px;color:var(--muted);line-height:1.45}\n  .sc-meth{font-size:10.5px;color:var(--faint);margin-top:7px}\n</style>\n\n<script>\n(function(){\n  const pts = {critical:12, high:5, medium:2, low:0.25};\n  const sevRank = {critical:4, high:3, medium:2, low:1};\n  const anchorMap = {4:75, 3:50, 2:28, 1:12, 0:0};\n\n  function compute(model){\n    const scan = (model && model.data_scan_results) || {};\n    const report = (model && model.data) || {};\n    const cr = scan.crossings || {};\n    const byProv = (scan.components && scan.components.summary && scan.components.summary.by_provider) || {};\n    const byType = (scan.components && scan.components.summary && scan.components.summary.by_type) || {};\n    const findings = report.findings || [];\n\n    const crit = cr.critical_count || 0;\n    const high = cr.high_count || 0;\n    const med  = cr.medium_count || 0;\n    const pickle = byProv.Pickle || 0;\n    const pulls = findings.filter(f => f.category === 'hf_import').length || 0;\n\n    const buckets = [\n      {sev:'critical', n:crit},\n      {sev:'high',     n:high},\n      {sev:'high',     n:pickle},\n      {sev:'medium',   n:med},\n      {sev:'low',      n:pulls}\n    ].filter(b => b.n > 0);\n\n    let raw = 0, worst = 0;\n    buckets.forEach(b=>{\n      raw += pts[b.sev] * Math.min(1 + Math.log(b.n), 5);\n      worst = Math.max(worst, sevRank[b.sev]);\n    });\n    const volume = 25 * (1 - Math.exp(-raw/30));\n    const anchor = anchorMap[worst] || 0;\n    const score = Math.round(Math.min(100, anchor + volume));\n\n    // context for headline\n    const reposScanned = report.repos_scanned || (Array.isArray(report.repo) ? report.repo.length : 0);\n    const reposAI = (report.inventory && report.inventory.repos_using_ai || []).length;\n    const crossings = cr.total_crossings || 0;\n    const artifacts = (byType.model_artifact && byType.model_artifact.count) || 0;\n    const scanId = (report.scan_name || '').slice(0,8);\n    const surfaces = (report.surfaces_run || []).map(s=> s==='cicd'?'CI/CD':s).join(' + ');\n\n    // describe the critical crossing\n    let critPhrase = 'a sensitive value reaching an AI boundary';\n    (scan.crossings && scan.crossings.crossings || []).some(c=>{\n      if(c.severity==='critical'){\n        if(c.data_category==='credentials') critPhrase='a database credential reaching an LLM prompt';\n        return true;\n      }\n      return false;\n    });\n\n    return {score, band:band(score), bandColor:bandColor(score),\n      reposScanned, reposAI, pulls, crossings, crit, artifacts, pickle,\n      scanId, surfaces, critPhrase};\n  }\n\n  function band(s){ return s<=20?'Low':s<=45?'Moderate':s<=70?'Elevated':s<=90?'High':'Critical'; }\n  function bandColor(s){ return s<=20?'var(--b-low)':s<=45?'var(--b-mod)':s<=70?'var(--b-elev)':s<=90?'var(--b-high)':'var(--b-crit)'; }\n\n  function render(model){\n    const d = compute(model);\n    const hasData = d.reposScanned || d.crossings || d.pulls;\n    if(!hasData){\n      document.getElementById('root').innerHTML = '<div class=\"aue-loading\">Waiting for scan data…</div>';\n      return;\n    }\n    const segs = [\n      {w:20,c:'var(--b-low)'},{w:25,c:'var(--b-mod)'},{w:25,c:'var(--b-elev)'},\n      {w:20,c:'var(--b-high)'},{w:10,c:'var(--b-crit)'}\n    ].map(s=>`<div class=\"seg\" style=\"width:${s.w}%;background:${s.c}\"></div>`).join('');\n\n    document.getElementById('root').innerHTML = `\n      <div class=\"aue-wrap\">\n        <div class=\"aue-top\">\n          <div>\n            <div class=\"aue-title\">AI Usage &amp; Exposure</div>\n            <div class=\"aue-prov\">\n              <span class=\"chip\">scan <b>${d.scanId||'—'}</b></span>\n              <span class=\"chip\">source <b>github</b></span>\n              <span class=\"chip\"><b>${d.reposScanned}</b> repos</span>\n              <span class=\"chip\">surfaces <b>${d.surfaces||'—'}</b></span>\n            </div>\n          </div>\n          <div class=\"phases\">\n            <span class=\"ph\"><span class=\"dot\"></span>Discovery <small>done</small></span>\n            <span class=\"arrow\">→</span>\n            <span class=\"ph\"><span class=\"dot\"></span>Posture <small>done</small></span>\n          </div>\n        </div>\n\n        <div class=\"aue-sub\">One scan, two phases — <b>Discovery</b> finds where AI is used; <b>Posture</b> scores how risky that use is. Discovery never emits a score; Posture does.</div>\n\n        <div class=\"aue-main\">\n          <div class=\"aue-headline\">\n            AI found in <span class=\"n\">${d.reposAI} of ${d.reposScanned}</span> repos\n            (<span class=\"n\">${d.pulls}</span> model pulls).\n            <span class=\"n\">${d.crossings}</span> flows carry sensitive data into AI boundaries —\n            <span class=\"crit\">${d.crit} critical</span>: ${d.critPhrase}.\n            <span class=\"n\">${d.artifacts}</span> model artifacts ship in code-executing formats\n            (<span class=\"warn\">${d.pickle} unsigned</span>); <span class=\"n\">${d.pulls}</span> pulls are unpinned.\n          </div>\n\n          <div class=\"scorecard\">\n            <div class=\"sc-label\">Posture Score</div>\n            <div class=\"sc-num\"><span style=\"color:${d.bandColor}\">${d.score}</span><small>/ 100</small></div>\n            <div class=\"sc-band\" style=\"background:${d.bandColor}\">${d.band}</div>\n            <div class=\"bandbar\">\n              ${segs}\n              <div class=\"marker\" style=\"left:${d.score}%\"></div>\n            </div>\n            <div class=\"bandscale\"><span>0</span><span>20</span><span>45</span><span>70</span><span>90</span><span>100</span></div>\n            <div class=\"sc-sub\">Severity-weighted · anchored by the single worst finding (1 critical credential → LLM leak).</div>\n            <div class=\"sc-meth\">Full formula in the methodology card at the bottom of the dashboard.</div>\n          </div>\n        </div>\n      </div>`;\n  }\n\nfunction boot(){\n    if (typeof appsmith === 'undefined'){ setTimeout(boot, 50); return; }\n    appsmith.onReady(function(){ render(appsmith.model); });\n    if (appsmith.onModelChange) appsmith.onModelChange(function(m){ render(m); });\n  }\n  boot();\n})();\n</script>",
    "js": "appsmith.onReady(() => {\n    appsmith.onModelChange((model) => {\n        const rawData = model.cookies || {};\n        const components = rawData.components || {};\n        const summary = components.summary || {};\n        const byType = summary.by_type || {};\n        const byProvider = summary.by_provider || {};\n        const crossFileData = rawData.cross_file || {};\n        const crossings = rawData.crossings || {};\n        const crossingList = crossings.crossings || [];\n        // =====================================================================\n        // CONFIG — the ONLY values not present in the scan data (presentation /\n        // display policy). The exposure index itself uses NO weighting coefficients.\n        // =====================================================================\n        const CONFIG = {\n            indexCap: 100,\n            indexThresholds: { high: 75, medium: 40 },\n            indexColors: { high: '#c0392b', medium: '#f39c12', low: '#27ae60' },\n            badgeBySeverity: { critical: 'badge-critical', high: 'badge-warning', medium: 'badge-warning', low: 'badge-warning' },\n            badgeDefault: 'badge-warning',\n            severityOrder: { critical: 0, high: 1, medium: 2, low: 3 },\n            maxActionRows: 5,\n            okColor: '#27ae60',\n            emptyFindings: 'No critical execution dependencies or boundary violations detected.',\n            emptyActions: 'No immediate actions required.',\n            // Serialization provider names — these carry \"High Exposure\" weight.\n            serializationTypes: ['Pickle', 'Joblib', 'H5', 'PT', 'Safetensors']\n        };\n        // --- Helpers ---------------------------------------------------------\n        const fmtCat = (s) => String(s || '')\n            .split('_')\n            .map(w => w.length <= 3 ? w.toUpperCase() : w.charAt(0).toUpperCase() + w.slice(1))\n            .join(' ');\n        const cap = (s) => s ? s.charAt(0).toUpperCase() + s.slice(1).toLowerCase() : s;\n        const sevRank = (s) => CONFIG.severityOrder[String(s || '').toLowerCase()] ?? 99;\n        const fmtProviders = (obj) => Object.entries(obj || {})\n            .map(([k, v]) => `${k} (${Array.isArray(v) ? v.length : v})`)\n            .join(', ');\n        // --- 1. Top-Level Metrics (all wired to the scan) --------------------\n        // AI boundary crossings is now defined as the SUM of the Sensitive Data\n        // Crossings (the by-category breakdown). This value flows into the card\n        // and the \"How this is calculated\" explanation so both stay consistent.\n        const byCategory = crossings.by_category || {};\n        const sensitiveDataCrossingsSum = Object.values(byCategory)\n            .reduce((acc, v) => acc + (Number(v) || 0), 0);\n        // If you meant the total instead of the category sum, use:\n        //   const boundaryCrossings = crossings.total_crossings || 0;\n        const boundaryCrossings = sensitiveDataCrossingsSum;\n        const eligibilityViolations = crossings.prohibited_count || 0;\n        const undeclaredComponents = (byType.llm_framework?.count || 0) + (byType.agent?.count || 0);\n        const artifactCount = byType.model_artifact?.count || 0;\n        // Exposed vendors = providers flagged \"High Exposure\" (serialization formats),\n        // consistent with the Vendor Exposure dashboard. A reference to a standard\n        // AI vendor / API is NOT itself an exposure, so those are excluded here.\n        let exposedVendorCount = 0;\n        Object.keys(byProvider).forEach((name) => {\n            if (CONFIG.serializationTypes.includes(name)) {\n                exposedVendorCount++;\n            }\n        });\n        // Overall Exposure Index — raw, unweighted sum of the five underlying counts,\n        // capped at 100. No coefficients: each item contributes its own count.\n        //   boundary + exposed vendors + framework + artifact + eligibility\n        const rawScore = boundaryCrossings + exposedVendorCount + undeclaredComponents + artifactCount + eligibilityViolations;\n        const indexScore = Math.min(CONFIG.indexCap, rawScore);\n        // KPI cards\n        document.getElementById('exec-exposure-index').innerText = `${indexScore}/${CONFIG.indexCap}`;\n        document.getElementById('exec-boundary-crossings').innerText = boundaryCrossings;\n        document.getElementById('exec-violations').innerText = eligibilityViolations;\n        document.getElementById('exec-artifacts').innerText = artifactCount;\n        const indexEl = document.getElementById('exec-exposure-index');\n        if (indexScore > CONFIG.indexThresholds.high) indexEl.style.color = CONFIG.indexColors.high;\n        else if (indexScore > CONFIG.indexThresholds.medium) indexEl.style.color = CONFIG.indexColors.medium;\n        else indexEl.style.color = CONFIG.indexColors.low;\n        // Explanation of the Overall Exposure Index (reconciles with the score above)\n        const exIndexFactors = [\n            ['AI boundary crossings', boundaryCrossings],\n            ['exposed vendors', exposedVendorCount],\n            ['undeclared frameworks/agents', undeclaredComponents],\n            ['model artifacts', artifactCount],\n            ['eligibility violations', eligibilityViolations]\n        ];\n        const exIndexBreakdown = exIndexFactors.map(([k, v]) => `${v} ${k}`).join(' + ');\n        const exCapNote = rawScore > CONFIG.indexCap ? ` (raw total ${rawScore}, capped at ${CONFIG.indexCap})` : '';\n        document.getElementById('exec-index-explanation').innerText =\n            `Unweighted sum of five scan counts — ${exIndexBreakdown} = ${rawScore}${exCapNote}.`;\n        // --- 2. Executive Findings (every specific derived from the scan) ----\n        const findingsList = document.getElementById('findings-list');\n        findingsList.innerHTML = '';\n        const findings = [];\n        const prohibited = crossingList.filter(c => c.ai_eligibility === 'prohibited');\n        if ((crossings.total_crossings || 0) > 0 && Object.keys(byCategory).length > 0) {\n            const catList = Object.entries(byCategory).map(([k, v]) => `${fmtCat(k)} (${v})`).join(', ');\n            findings.push(`<strong>Sensitive Data Crossings:</strong> ${crossings.total_crossings} flow(s) carry restricted data into AI boundaries — by category: ${catList}.`);\n        }\n        const byBoundary = crossings.by_boundary || {};\n        if (boundaryCrossings > 0 || Object.keys(byBoundary).length > 0) {\n            const bnames = Object.keys(byBoundary).map(fmtCat).join(', ') || 'n/a';\n            findings.push(`<strong>Boundary Exposure:</strong> ${boundaryCrossings} AI boundary node(s) detected; sensitive data observed at: ${bnames}.`);\n        }\n        if (artifactCount > 0) {\n            const artProv = fmtProviders(byType.model_artifact?.providers);\n            findings.push(`<strong>Supply Chain Risk:</strong> ${artifactCount} model artifact(s) using serialization formats exposed to code execution — ${artProv}.`);\n        }\n        if (findings.length === 0) {\n            findingsList.innerHTML = `<li style=\"color: ${CONFIG.okColor};\">${CONFIG.emptyFindings}</li>`;\n        } else {\n            findings.forEach(f => {\n                const li = document.createElement('li');\n                li.innerHTML = f;\n                findingsList.appendChild(li);\n            });\n        }\n        // --- 3. High Priority Action Items (driven entirely by crossings) ----\n        const tbody = document.getElementById('exec-table-body');\n        tbody.innerHTML = '';\n        const actionItems = crossingList\n            .slice()\n            .sort((a, b) => sevRank(a.severity) - sevRank(b.severity))\n            .slice(0, CONFIG.maxActionRows)\n            .map(c => ({\n                cat: fmtCat(c.data_category) + (c.ai_eligibility ? ` (${c.ai_eligibility})` : ''),\n                ctx: `${c.file}:${c.line}`,\n                severity: cap(c.severity),\n                badge: CONFIG.badgeBySeverity[String(c.severity || '').toLowerCase()] || CONFIG.badgeDefault,\n                action: c.recommendation || '—'\n            }));\n        if (actionItems.length === 0) {\n            tbody.innerHTML = `<tr><td colspan=\"4\" style=\"text-align: center;\">${CONFIG.emptyActions}</td></tr>`;\n            return;\n        }\n        actionItems.forEach(item => {\n            const tr = document.createElement('tr');\n            const tdCat = document.createElement('td');\n            tdCat.innerHTML = `<strong>${item.cat}</strong>`;\n            const tdCtx = document.createElement('td');\n            const code = document.createElement('code');\n            code.textContent = item.ctx;\n            tdCtx.appendChild(code);\n            const tdSeverity = document.createElement('td');\n            const span = document.createElement('span');\n            span.className = `badge ${item.badge}`;\n            span.textContent = item.severity;\n            tdSeverity.appendChild(span);\n            const tdAction = document.createElement('td');\n            tdAction.textContent = item.action;\n            tr.appendChild(tdCat);\n            tr.appendChild(tdCtx);\n            tr.appendChild(tdSeverity);\n            tr.appendChild(tdAction);\n            tbody.appendChild(tr);\n        });\n    });\n});"
  },
  "version": 1,
  "widgetId": "9uobksuvu8",
  "widgetName": "Custom6"
}